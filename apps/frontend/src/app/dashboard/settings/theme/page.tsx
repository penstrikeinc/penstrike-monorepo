// ----------------------------------------------------------------------

import { Settings } from 'src/sections';

export const metadata = {
  title: 'Dashboard: Six',
};

export default function Page() {
  return <Settings />;
}
